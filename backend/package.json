{
  "name": "tgeapcet-backend",
  "version": "1.0.0",
  "description": "TG EAPCET College Predictor - Production Backend API",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npx prisma generate",
    "postinstall": "npx prisma generate",
    "seed": "node seed.js",
    "db:migrate": "npx prisma migrate dev",
    "db:push": "npx prisma db push",
    "db:studio": "npx prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.9.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "prisma": "^5.9.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
